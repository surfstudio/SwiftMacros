import SwiftSyntaxMacros
import SwiftSyntaxMacrosTestSupport
import XCTest

#if canImport(SurfMacroBody)
import SurfMacroBody

// uncomment the line bellow when there is an implementation of the macro
// private let testMacros: [String: Macro.Type] = ["{{macro_name}}": {{macro_type}}.self]
#endif

final class {{macro_type}}Tests: XCTestCase {}
