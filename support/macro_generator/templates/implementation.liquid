import SwiftCompilerPlugin
import SwiftSyntax
import SwiftSyntaxBuilder
import SwiftSyntaxMacros

//select a Macro protocol based on your macroDefinition

//public struct {{ macro_type }}: <macro_protocol> {
   // implement the expansion function here, based on the chosen Macro protocol or protocols
//}
